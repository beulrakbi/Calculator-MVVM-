<UserControl x:Class="Calculator.View.History"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Calculator.View"
             xmlns:vm="clr-namespace:Calculator.ViewModel" d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="100">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      Background="White">
            <ListBox ItemsSource="{Binding HistoryList}"
                     BorderThickness="0"
                     HorizontalAlignment="Right"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled">

                <ListBox.Style>
                    <Style TargetType="ListBox">
                        <Setter Property="Background" Value="White"/>
                    </Style>
                </ListBox.Style>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel VerticalAlignment="Bottom"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel HorizontalAlignment="Right">
                            <TextBlock Text="{Binding Equation}" FontSize="16" Foreground="black" HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding Result}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>



                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </ScrollViewer>

        <Grid Grid.Row="1" 
              Background="White"
              >
            <Button Content="Crear"
                    FontSize="20"
                    Background="White"
                    Foreground="black"
                    HorizontalAlignment="Right"
                    BorderThickness="0"
                    
                />
        </Grid>
    </Grid>
</UserControl>
